# Canonical line endings: LF for all text files in the repo.
# Git auto-detects text vs. binary and stores text as LF on commit.
# Windows users can opt into CRLF in their working tree via core.autocrlf=true.
* text=auto eol=lf
